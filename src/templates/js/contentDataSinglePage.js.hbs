var objData = {
	pathToRoot: "../../../",
	listing: [
		{
			folderName: "build",
			title: "{{global.previewTitle}}",
			folderList: [
				{{~#each this.english}}
        {	
					folderName: "html",
					title: "{{@key}} - HTML Standard Media",
					bannerList: [
            {{#each this}}
            {{~#unless @first}}
						{
							overwriteSizeDisplay: "", 
							label: "{{name}}",{{log name}}
							src: "{{name}}",
							w: {{width}},
							h: {{height}},
							type: "html5",
							approved: {{approved}},
							dload: true,
							backup: "{{backup}}",
							manualzip: true
						}
						{{~#unless @last}},{{/unless}}
						{{/unless}}
            {{/each}}
					]
				}{{#unless @last}},{{/unless}}
      {{/each}}
			]
		}{{#unless @last}}, {{/unless}}
	]
}